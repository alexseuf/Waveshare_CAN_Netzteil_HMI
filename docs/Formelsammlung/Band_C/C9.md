---
title: "Band C – Magnetische Auslegung"
chapter: "C9"
section: "Reproduzierbare Diagrammerzeugung"
---

# Band C – Magnetische Auslegung

## C9 Reproduzierbare Diagrammerzeugung

### Ziel

Dieses Kapitel archiviert die Gleichungen, Parameter und Python-Referenzimplementierungen, mit denen die Diagramme der PFC-Drossel-Dokumentation reproduzierbar erzeugt werden können. Für eine neue Drosselvariante werden ausschließlich die Werte in `C9_Skripte/parameter.py` angepasst. Anschließend erzeugt `generate_all_figures.py` sämtliche SVG- und PNG-Dateien neu.

### Verzeichnisstruktur

```text
Band_C/
├── C9.md
└── C9_Skripte/
    ├── parameter.py
    ├── generate_all_figures.py
    └── README.md
```

### Abhängigkeiten

```bash
python -m pip install numpy matplotlib
```

### Aufruf

```bash
cd docs/Formelsammlung/Band_C/C9_Skripte
python generate_all_figures.py
```

Optional kann ein anderes Ausgabeverzeichnis angegeben werden:

```bash
python generate_all_figures.py --output ../../../../Drossel_Spezifikationen/Beispiele/MEIN_BEISPIEL/Bilder
```

## C9.1 Parameter und Einheiten

Im Skript werden SI-Einheiten verwendet:

| Größe | Einheit im Skript |
|---|---|
| Länge | m |
| Querschnitt | m² |
| Strom | A |
| Spannung | V |
| Frequenz | Hz |
| Induktivität | H |
| Flussdichte | T |
| Verlustleistung | W |
| Temperatur | °C beziehungsweise K |

Die Parameterdatei enthält unter anderem:

- Kernabmessungen und Stapelzahl,
- Windungszahl,
- Litzenaufbau,
- $L_{diff}(I)$-Stützstellen,
- $B(I)$-Stützstellen,
- Netz- und Zwischenkreisspannung,
- Schaltfrequenz,
- Lastfälle,
- Steinmetz-Parameter,
- Wicklungswiderstand und Temperaturkoeffizient,
- thermische Ersatzgrößen.

## C9.2 Kerngeometrie

Für einen Ringkern gilt näherungsweise

```math
A_e=h\,\frac{D_a-D_i}{2}
```

und

```math
l_e=\pi\,\frac{D_a+D_i}{2}.
```

Bei $n_K$ identischen gestapelten Kernen wird

```math
A_{e,ges}=n_K A_e,
```

während die effektive magnetische Weglänge näherungsweise unverändert bleibt.

Das Kernvolumen lautet

```math
V_e=A_e l_e.
```

## C9.3 Anfangsinduktivität

```math
L_0=\frac{\mu_0\mu_{r0}N^2A_e}{l_e}.
```

Für nichtlineare Kennlinien gilt die Randbedingung

```math
L_{diff}(0)=L_{sec}(0)=L_0.
```

## C9.4 Magnetische Kennlinien

Die Feldstärke wird aus dem Drosselstrom berechnet:

```math
H(I)=\frac{NI}{l_e}.
```

Falls Herstellerdaten in Oersted vorliegen:

```math
H[\mathrm{Oe}]=\frac{4\pi NI}{l_e[\mathrm{mm}]}.
```

Die Flussverkettung wird aus der differentiellen Induktivität numerisch integriert:

```math
\Psi(I)=\int_0^I L_{diff}(i)\,di.
```

Daraus folgt

```math
L_{sec}(I)=\frac{\Psi(I)}{I}
=\frac{1}{I}\int_0^I L_{diff}(i)\,di.
```

Am Ursprung wird der Grenzwert eingesetzt:

```math
L_{sec}(0)=L_{diff}(0)=L_0.
```

Die relativen Permeabilitäten werden auf $L_0$ normiert:

```math
\mu_{r,diff}(I)=\mu_{r0}\frac{L_{diff}(I)}{L_0}
```

```math
\mu_{r,sec}(I)=\mu_{r0}\frac{L_{sec}(I)}{L_0}.
```

Die Flussdichte kann entweder direkt aus einer Herstellerkennlinie $B(H)$ beziehungsweise $B(I)$ interpoliert oder aus der Flussverkettung berechnet werden:

```math
B(I)=\frac{\Psi(I)}{N A_e}.
```

## C9.5 Phasenspannung, Tastgrad und Grundwellenstrom

Für ein symmetrisches dreiphasiges Netz gilt

```math
\hat U_{Phase}=\frac{\sqrt{2}\,U_{LL,rms}}{\sqrt{3}}.
```

```math
u_{Phase}(t)=\hat U_{Phase}\sin(2\pi f_{Netz}t).
```

Das vereinfachte phasenbezogene Zweilevel-PWM-Modell verwendet

```math
d(t)=0{,}5+\frac{u_{Phase}(t)}{U_{DC}}.
```

Der Grundwellenstrom lautet

```math
i_1(t)=\sqrt{2}\,I_{1,rms}\sin(2\pi f_{Netz}t).
```

## C9.6 PWM-Stromwelligkeit

Mit der differentiellen Induktivität am momentanen Arbeitspunkt gilt

```math
\Delta I_{pp}(t)=
\frac{U_{DC}\,d(t)[1-d(t)]}
{L_{diff}(|i_1(t)|)f_s}.
```

Für die Vergleichsrechnung mit konstanter Anfangsinduktivität wird $L_{diff}$ durch $L_0$ ersetzt.

Die obere und untere Hüllkurve lauten

```math
i_{oben}(t)=i_1(t)+\frac{\Delta I_{pp}(t)}{2}
```

und

```math
i_{unten}(t)=i_1(t)-\frac{\Delta I_{pp}(t)}{2}.
```

## C9.7 Zeitdiskrete Überlagerung mit dem 70-kHz-Dreieck

Der normierte symmetrische Dreiecksträger besitzt den Wertebereich $-1\ldots+1$:

```math
\mathrm{tri}(t)=4\left|\left(f_s t\bmod 1\right)-\frac{1}{2}\right|-1.
```

Der vollständige Phasenstrom wird zeitdiskret berechnet:

```math
i(t)=i_1(t)+\frac{\Delta I_{pp}(t)}{2}\,\mathrm{tri}(t).
```

Die zeitliche Schrittweite muss deutlich kleiner als die Schaltperiode sein. Das Referenzskript verwendet standardmäßig 100 Punkte pro Schaltperiode:

```math
\Delta t=\frac{1}{100f_s}.
```

Die Maxima und Minima des Gesamtstroms werden aus der diskreten Kurve bestimmt und nicht durch einfache Addition unabhängiger Maximalwerte abgeschätzt.

## C9.8 Flussdichtehub

```math
\Delta B_{pp}(t)=\frac{L_{diff}(t)\Delta I_{pp}(t)}{NA_e}.
```

Nach Einsetzen der Stromwelligkeit kürzt sich $L_{diff}$ heraus:

```math
\Delta B_{pp}(t)=
\frac{U_{DC}d(t)[1-d(t)]}{NA_ef_s}.
```

Die hochfrequente Flussdichteamplitude beträgt

```math
B_{pk}(t)=\frac{\Delta B_{pp}(t)}{2}.
```

Die konservative maximale Gesamtflussdichte wird berechnet aus

```math
B_{max,gesamt}=B_{GW}+\frac{\Delta B_{pp,max}}{2}.
```

## C9.9 Kupferverluste

Der Gleichstromwiderstand bei Temperatur $T$ lautet

```math
R(T)=R_{25}\left[1+\alpha_{Cu}(T-25\,^{\circ}\mathrm C)\right].
```

Die DC-Kupferverluste ergeben sich zu

```math
P_{Cu,DC}=I_{rms}^2R(T).
```

Falls ein AC-Widerstandsfaktor verwendet wird:

```math
P_{Cu}=I_{rms}^2R(T)k_{AC}.
```

Für eine genaue Auswertung sind Grundwelle und hochfrequente Stromanteile getrennt zu behandeln.

## C9.10 Kernverluste

Die im Beispiel verwendete Steinmetz-Gleichung lautet

```math
P_v=a\,B_{pk}^{b}f_{kHz}^{c}.
```

Dabei wird $P_v$ in $\mathrm{mW/cm^3}$, $B_{pk}$ in T und $f_{kHz}$ in kHz eingesetzt. Die gesamte Kernverlustleistung ist

```math
P_{core}=P_vV_e.
```

Bei zeitabhängigem $B_{pk}(t)$ wird der Mittelwert der momentanen Verlustdichte über eine Netzperiode gebildet.

## C9.11 Gesamtverluste und Thermik

```math
P_{ges}=P_{Cu}+P_{core}.
```

Der stationäre Temperaturanstieg lautet

```math
\Delta T_{stat}=P_{ges}R_{th}.
```

Für eine kurze adiabatische Belastung gilt

```math
\Delta T_{ad}=\frac{P\,\Delta t}{C_{th}}.
```

## C9.12 Erzeugte Abbildungen

Das Referenzskript erzeugt folgende Dateien:

| Datei | Inhalt |
|---|---|
| `abbildung_01_mechanischer_aufbau.svg` | gestapelter Ringkern und Anschlussdarstellung |
| `abbildung_02_wicklungsaufbau.svg` | schematischer Lagenaufbau |
| `abbildung_03_bh_kennlinie.svg` | $B(H)$ beziehungsweise $B(I)$ |
| `abbildung_04_induktivitaet.svg` | $L_{diff}(I)$ und $L_{sec}(I)$ |
| `abbildung_05_permeabilitaet.svg` | $\mu_{r,diff}(I)$ und $\mu_{r,sec}(I)$ |
| `abbildung_06_kupferverluste.svg` | Kupferverluste bei zwei Temperaturen |
| `abbildung_07_kernverluste.svg` | Kernverlust über Netzwinkel beziehungsweise Strom |
| `abbildung_08_gesamtverluste.svg` | Gesamtverlustkennlinie |
| `abbildung_09_temperaturanstieg.svg` | adiabatische Temperaturerhöhung |
| `abbildung_09_stromverlauf_50Hz_70kHz.svg` | zeitdiskreter Stromverlauf mit Dreiecksrippel |
| `abbildung_10_stromwelligkeit_netzwinkel.svg` | Stromwelligkeit mit konstantem $L_0$ |
| `abbildung_11_stromwelligkeit_ldiff_grundwelle.svg` | Stromwelligkeit mit $L_{diff}(I)$ |
| `abbildung_12_flussdichtehub_ldiff_grundwelle.svg` | Flussdichtehub |
| `abbildung_13_ausnutzung_kernmaterial_bmax.svg` | Ausnutzung des Kernmaterials |

Zu jeder SVG-Datei wird zusätzlich eine PNG-Datei erzeugt.

## C9.13 Anpassung an eine neue Drossel

1. `parameter.py` kopieren und alle Geometrie-, Material-, Wicklungs- und Systemparameter ändern.
2. $L_{diff}(I)$- und $B(I)$-Stützstellen aus Datenblatt, Messung oder Simulation eintragen.
3. Plausibilitätsprüfung ausführen:
   - $L_{diff}(0)=L_0$,
   - Stromstützstellen streng monoton,
   - positive Induktivitäten,
   - $0<d(t)<1$,
   - ausreichend hohe Zeitauflösung.
4. `generate_all_figures.py` ausführen.
5. erzeugte Kennwertdatei `berechnete_kennwerte.csv` prüfen.
6. SVG-Dateien in das jeweilige Dokumentationsverzeichnis übernehmen.

## C9.14 Grenzen des Referenzmodells

- Das PWM-Modell ist phasenbezogen und vereinfacht. Der reale Schaltzustandsverlauf einer SVM kann abweichen.
- Die lineare Interpolation der Kennlinien ersetzt kein validiertes Materialmodell.
- Die Steinmetz-Gleichung berücksichtigt ohne Erweiterung keine DC-Bias-Abhängigkeit.
- AC-Kupferverluste benötigen für belastbare Ergebnisse eine harmonische, analytische, FEM- oder messtechnische Auswertung.
- Mechanische Zeichnungen sind schematisch und keine Fertigungszeichnungen.
- Die endgültige Auslegung ist durch PLECS-/SPICE-Simulation, Messung und thermische Prüfung zu verifizieren.

## C9.15 Archivierte Skripte

- [`C9_Skripte/parameter.py`](C9_Skripte/parameter.py)
- [`C9_Skripte/generate_all_figures.py`](C9_Skripte/generate_all_figures.py)
- [`C9_Skripte/README.md`](C9_Skripte/README.md)
